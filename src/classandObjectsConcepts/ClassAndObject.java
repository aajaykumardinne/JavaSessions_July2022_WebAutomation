package classandObjectsConcepts;

/**
 * 
 * @author Aajay Kumar Dinne
 *
 */
public class ClassAndObject {

	public static void main(String[] args) {

		// class is the blueprint /category/template of the objects
//		Example: page is the template  hence we create different page classes
		// Object : physical entity or instance of the class

		// Example :
		// 1.Humans is the class , Aajay is the object
		// 2.Laptops is the class , keyboard is the object

		// every object has the reference name + class properties

		// class properties are defined by the class

		// create the object of the class using the new keyword

		// objects will be created in the heap memory

		// object reference names will be created in the stack memory

		// can we create the object without any reference?
		// yes we can

		// can we create the null object reference?
		// yes we can

	}

}
